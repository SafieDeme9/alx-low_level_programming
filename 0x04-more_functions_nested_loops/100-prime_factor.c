#include <stdio.h>

/**
 * main - prime numbers
 *
 * Return: 0
 */

int main(void)
{
	long num = 612852475143;
	long div;

	for (div = 2; div < num; div++)
	{
		while (num % div == 0)
		{
			num = num / y;
		}
	}
	printf("%lu", y);
	putchar('\n');
	returm (0);
}
